# نظام تحليل ملفات CSV

نظام ويب بسيط لتحليل ملفات CSV باستخدام Streamlit.

## المميزات

- ✅ رفع ملفات CSV بسهولة
- 📊 عرض إحصائيات شاملة:
  - عدد الصفوف والأعمدة
  - عدد البيانات الفارغة
  - عدد الصفوف المكررة
- 🔍 تحليل تفصيلي للبيانات الفارغة
- 👁️ معاينة البيانات

## التثبيت

1. تثبيت المكتبات المطلوبة:
```bash
pip install -r requirements.txt
```

## التشغيل

قم بتشغيل التطبيق باستخدام الأمر التالي:

```bash
streamlit run streamlist.py
```

سيتم فتح التطبيق في المتصفح تلقائياً على العنوان: `http://localhost:8501`

## الاستخدام

1. افتح التطبيق في المتصفح
2. اضغط على "Browse files" لاختيار ملف CSV
3. سيتم عرض الإحصائيات والتحليل تلقائياً


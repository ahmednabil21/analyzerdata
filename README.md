# نظام تحليل ملفات CSV (Flask)

تطبيق ويب مبني بإطار عمل **Flask** لتحليل ملفات CSV بطريقة سهلة وسريعة، مع واجهة عربية كاملة.

## المميزات الرئيسية

- ✅ رفع ملفات CSV وحفظها أثناء الجلسة
- 📊 إحصائيات شاملة:
  - عدد الصفوف والأعمدة
  - عدد الصفوف المكررة
  - إجمالي ونسبة البيانات الفارغة
  - عدد الأعمدة المكتملة (مع استثناء الحقول المحددة)
- ✅ قسم متقدم لمتابعة حالة كل عمود مع إمكانية البحث بالاسم
- 🔎 البحث عن كلمة داخل البيانات مع عرض مرات الظهور وأمثلة فعلية
- 👁️ معاينة للصفوف العشرة الأولى من الملف
- ℹ️ عرض أسماء الأعمدة وأنواع البيانات بالتفصيل

## المتطلبات

- Python 3.9 أو أحدث
- تثبيت المتطلبات:

```bash
pip install -r requirements.txt
```

## تشغيل محلي

```bash
flask --app flask_app run --debug
```

سيفتح التطبيق على عنوان `http://127.0.0.1:5000`. يمكنك تعطيل خيار `--debug` في بيئة الإنتاج.

## النشر على Plesk أو أي خادم WSGI

1. ارفع الملفات التالية على الخادم:
   - `flask_app.py`
   - مجلد `templates/`
   - مجلد `static/`
   - `requirements.txt`
2. في لوحة Plesk أنشئ تطبيق Python جديد وحدّد:
   - ملف التطبيق (Application Startup File): `flask_app.py`
   - أمر البدء (WSGI script): `app`
3. ثبّت المتطلبات داخل بيئة التطبيق:

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

4. أعد تشغيل التطبيق من لوحة التحكم.

## الاستخدام

1. افتح الصفحة الرئيسية للتطبيق.
2. ارفع ملف CSV (حد أقصى 50 ميجابايت).
3. استعرض الإحصائيات، حالة الأعمدة، والبحث عن كلمات داخل البيانات.
4. استخدم زر "إزالة الملف الحالي" لبدء تحليل ملف جديد.

